# Variables leídas por docker-compose.yml para el servicio postgres.
# Deben coincidir con las credenciales usadas en backend/.env (DATABASE_URL).
# Copia este fichero como .env en la raíz del repo.

POSTGRES_DB=ticketing_db
POSTGRES_USER=ticketing_user
POSTGRES_PASSWORD=ticketing_pass
